version: '3.8'

services:
  frontend:
    container_name: react-frontend
    build:
      context: .
      dockerfile: .Dockerfile
    ports:
      - "80:80"
    # volumes:
    #   - ./nginx.conf:/etc/nginx/nginx.conf
    restart: always
